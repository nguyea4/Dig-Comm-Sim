Each function hsa their own respective test function

main 
-> audioencoding: encodes a signal with sample frequency into an encoded bitstream. Also has the dictionary used to encode the respective code rate
-->lempelzivencoding: converts the signal into an uncoded bitstream and then into an encoded bitstream